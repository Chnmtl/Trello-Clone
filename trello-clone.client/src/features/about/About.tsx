import { Typography, Box } from '@mui/material';

const About = () => (
  <Box p={2}>
    <Typography variant="h4" gutterBottom>About</Typography>
    <Typography>This is a simple Trello clone app. You can manage tasks in columns, drag and drop, and more features coming soon!</Typography>
  </Box>
);

export default About;
